{
    "name": "riot",
    "description": "AWS IoT Shadow service controlled light fixture ",
    "version": "0.0.1",
    "dependencies": {
        "request": "^2.88.0"
    },
    "node-red": {
        "settings": {
            "flowFile": "flow.json",
            "credentialsFile": "flow_cred.json"
        }
    }
}
